# Large Files / Data to Download

The following large files and directories were excluded from this repository to keep the size manageable. You can download them separately from the provided Google Drive link (User to add link here).

## EvoMed (Medical Diagnosis System)
*   `guilin_inpatient_extracted_10000.xlsx` (Place in `evomed/`)
*   `rag/` (Directory containing `rag_build.py`, `rag_index/` and guidelines. Place in `evomed/rag/`)
*   `exp/A-mem-sys/A-mem-sys/memory_db/` (Directory containing ChromaDB files. Place in `evomed/exp/A-mem-sys/A-mem-sys/memory_db/`)
*   `exp/ACE-open-main/reports/` (Large report snapshots. Place in `evomed/exp/ACE-open-main/reports/`)
*   `exp/ACE-open-main/case_library/` (Large case library JSONL files. Place in `evomed/exp/ACE-open-main/case_library/`)

## EvoMem (ACE Framework)
*   `test.xlsx` (Original name: `guilin_100K_20K_诊断_20260102_161948.xlsx`. Place in `evomed/evomem/`)

## Instructions
1. Download the files from Google Drive.
2. Place them in their respective directories as indicated above to restore full functionality.

